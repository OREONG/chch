<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberns">

	<!-- SB 전체 회원 수 구하기 -->
	<select id="getTotal" resultType="int">
		select count(*) from member
	</select>
	
	<!-- SB 회원 페이징 -->
	<select id="memberList" parameterType="map" resultType="member">
		select * from (select rowNum rn, a.* from (select * from member order by email)a) where rn between #{startRow} and #{endRow}
	</select>
	
	<update id="adminDelete" parameterType="map">
		update member set del=#{del} where email=#{email}
	</update>
	
</mapper>